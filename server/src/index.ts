/**
 * The Lambda entry point. Issue 10 replaces this with the handler that
 * discriminates scheduled events from HTTP events before anything else.
 */

export { IDEMPOTENCY_HEADER } from "@betterwakeup/contract";
